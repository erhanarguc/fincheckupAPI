@page

@model IndexModel
@{
    ViewData["Title"] = "Giriş Ekranı";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

@using fincheckup.Models.NKolay.json
@using fincheckup.ENTITY
@using fincheckup.Models.ViewM
@using fincheckup.Models.Hvvn
@using System.Globalization
@functions {
    public class IndexModel : PageModel
    {
        public IEnumerable<YearResult> myearResult;
        public long UserID;
        public long CompID;
        public int CompCount;
        public int YearCount;
        public HhvnUsers CurrentUser;
        public DashYearlyBilancoChart ncart;
        public List<DashPivotView> nRequestList;

        public List<DashPivotView> nRequestListChk;
        public IEnumerable<Companies> mreqListCompany;
        public string NetIsletme { get; set; }
        public string CompName { get; set; }
        public string CariOran { get; set; }
        public decimal CariOranT { get; set; }
        public string NakitOran { get; set; }
        public double val1 { get; set; }
        public double val3 { get; set; }
        public void OnGet()
        {




            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));

            mreqListCompany = Companies.Getby_User(UserID);

            CompID = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault().ID;
            CompName = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault().CompanyName;
            HhvnUsers.SetYearMain(CompID, UserID);
            CurrentUser = HhvnUsers.GetRow_User(UserID);
            YearCount = TBLXml.GetYearByComapnyID(CompID);
            CompCount = mreqListCompany.Count();

            myearResult = YearResult.getValue();

            DashGelirTablosu.Get_MAINRESULTMultiMainPIVOTCheck(CompID);
            //nRequestListTx = DashBilancoViewTx.setDashBilanco(nRequestList);

            //ncart = new DashYearlyBilancoChart();
            //ncart.SetResult(nRequestList, CurrentUser.SelectedYear);


            // nRequestListChk = nRequestList;
            //DashGelirTablosuViewMain.getList(CurrentUser.SelectedYear, CompID);

            //val1 = nRequestListChk.Where(x => x.TypeID == 111).Sum(x => x.Amount);
            //val3 = nRequestListChk.Where(x => x.TypeID == 2222).Sum(x => x.Amount);
            //if (val3 < 0) { val3 = val3 * -1; }
            //NetIsletme = (val1 - val3).ToString("0,0.00", new CultureInfo("en-US", false));

            //// DashBilancoViewMain.getNetIsletme(CurrentUser.SelectedYear, CompID);
            //var val5 = nRequestListChk.Where(x => x.TypeID == 333).Sum(x => x.Amount);
            //if (val5 == 0)
            //{
            //    val5 = 1;
            //}
            //CariOranT = (Convert.ToDecimal(nRequestListChk.Where(x => x.TypeID == 111).Sum(x => x.Amount)) / Convert.ToDecimal(val5));
            //if (CariOranT < 0)
            //{
            //    CariOranT = CariOranT * -1;
            //}
            //CariOran = CariOranT.ToString("0,0.00", new CultureInfo("en-US", false));
            ////DashBilancoViewMain.getCariOran(CurrentUser.SelectedYear, CompID).ToString("F");
            //NakitOran = (Convert.ToDecimal(nRequestListChk.Where(x => (x.TypeID == 10 || x.TypeID == 11)).Sum(x => x.Amount)) / Convert.ToDecimal(val5)).ToString("N2");
            //DashBilancoViewMain.getNakitOran(CurrentUser.SelectedYear, CompID).ToString("F");
        }

      
        public JsonResult OnGetGraphYear(int nyear)
        {
            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            HhvnUsers.SetYear(nyear, UserID);
            return new JsonResult("ok");
        }
        public JsonResult OnGetChartRasyo(DataSourceLoadOptions options)
        {
            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            CurrentUser = HhvnUsers.GetRow_User(UserID);

            CompID = Companies.Getby_User(UserID).Where(x => x.IsDefault == 1).FirstOrDefault().ID;
            nRequestList = DashGelirTablosu.Get_MAINRESULTMultiMainPIVOT(CompID).Where(x => x.IsHidden == 0).ToList();
            //nRequestListTx = DashBilancoViewTx.setDashBilanco(nRequestList);

            ncart = new DashYearlyBilancoChart();
            //ncart.SetResult(nRequestList, CurrentUser.SelectedYear);

            return new JsonResult(DataSourceLoader.Load(nRequestList, options));
        }
        public JsonResult OnGetMarkupMarjin(DataSourceLoadOptions options, long compid)
        {
            List<DashBilancoViewMulti> chklist = new List<DashBilancoViewMulti>();
            //if (string.IsNullOrEmpty(myear))
            //{
            //    return new JsonResult(DataSourceLoader.Load(chklist, options));
            //}


            //string[] llist = myear.Split(',');
            //if (llist.Length < 0)
            //{
            //    return new JsonResult(DataSourceLoader.Load(chklist, options));
            //}
            //var tt = llist.Select(int.Parse).ToList();

            var chk = DashGelirTablosu.Get_MAINRESULTMultiMainPIVOT(compid).Where(x => x.IsHidden == 0);
            return new JsonResult(DataSourceLoader.Load(chk, options));
        }
        public JsonResult OnGetMarkupMarjinA(DataSourceLoadOptions options, long compid)
        {
            List<DashBilancoViewMulti> chklist = new List<DashBilancoViewMulti>();
            //if (string.IsNullOrEmpty(myear))
            //{
            //    return new JsonResult(DataSourceLoader.Load(chklist, options));
            //}


            //string[] llist = myear.Split(',');
            //if (llist.Length < 0)
            //{
            //    return new JsonResult(DataSourceLoader.Load(chklist, options));
            //}
            //var tt = llist.Select(int.Parse).ToList();

            var chk = DashGelirTablosu.Get_MAINRESULTMultiMainPIVOTA(compid).Where(x => x.IsHidden == 0).OrderBy(x=>x.CounterZone);
            return new JsonResult(DataSourceLoader.Load(chk, options));
        }
        public JsonResult OnGetMarkupMarjinB(DataSourceLoadOptions options, long compid)
        {
            List<DashBilancoViewMulti> chklist = new List<DashBilancoViewMulti>();
            //if (string.IsNullOrEmpty(myear))
            //{
            //    return new JsonResult(DataSourceLoader.Load(chklist, options));
            //}


            //string[] llist = myear.Split(',');
            //if (llist.Length < 0)
            //{
            //    return new JsonResult(DataSourceLoader.Load(chklist, options));
            //}
            //var tt = llist.Select(int.Parse).ToList();

            var chk = DashGelirTablosu.Get_MAINRESULTMultiMainPIVOTB(compid).Where(x => x.IsHidden == 0).OrderBy(x => x.CounterZone);
            return new JsonResult(DataSourceLoader.Load(chk, options));
        }
      
    }
}
@section Styles {
    <link rel="stylesheet" href="/assets/plugins/jquery-datatable/dataTables.bootstrap4.min.css">
    <link rel="stylesheet" href="/assets/plugins/morrisjs/morris.css">
    <style>
        .tdnormall {
            padding-top: 0.1rem !important;
            padding-left: 0.75rem !important;
            padding-bottom: 0.1rem !important;
            padding-right: 0.75rem !important;
        }

        #chartxt {
            max-height: 255px !important;
        }

        #pivotgrid td {
            vertical-align: middle;
        }

        #pivotgrid .chart-cell {
            overflow: visible;
        }

        #pivotgrid .current-value {
            display: inline-block;
        }

        #pivotgrid .diff {
            position: relative;
            display: table-cell;
            font-size: 16px;
        }

        #pivotgrid .inc .diff {
            color: #2ab71b;
        }

        #pivotgrid .inc .diff:before {
                content: '+';
            }

        #pivotgrid .dec .diff:before {
            content: '-';
        }

        #pivotgrid .dec .diff {
            color: #f00;
        }

            #pivotgrid .inc .diff:after,
        #pivotgrid .dec .diff:after {
                content: '';
                display: block;
                height: 10px;
                width: 10px;
                position: absolute;
                right: -12px;
                top: 6px;
                background-repeat: no-repeat;
                background-size: 10px 10px;
            }

        #pivotgrid .inc .diff:after {
            background-image: url('data:image/png;base64,');
        }

        #pivotgrid .dec .diff:after {
            background-image: url('data:image/png;base64,');
        }
    </style>
}
<section class="content home" id="employee">
    @(Html.DevExtreme().LoadPanel()
        .ID("loadPanel")
        .ShadingColor("rgba(0,0,0,0.4)")
        .Position(p => p.Of("#employee"))
        .Visible(false)
        .ShowIndicator(true)
        .ShowPane(true)
        .Shading(true)
        .HideOnOutsideClick(false)
        .OnShown("loadPanel_shown")
        )

    <input type="hidden" style="display:none" id="txtmyear" value="@Model.CurrentUser.SelectedYear" />
    <input type="hidden" style="display:none" id="txtmcomp" value="@Model.CompID" />
    <input type="hidden" style="display:none" id="txtyearcount" value="@Model.YearCount" />
    <input type="hidden" style="display:none" id="txtcompcount" value="@Model.CompCount" />
    <div class="container-fluid">
        <div class="block-header">
            <div class="row clearfix">
                <div class="col-lg-5 col-md-5 col-sm-12">
                    <h2>Bilanço</h2>
                    <ul class="breadcrumb padding-0">
                        <li class="breadcrumb-item"><a href=""><i class="zmdi zmdi-home"></i></a>Gelir Tablosu</li>
                        <li class="breadcrumb-item active">Revenue</li>
                    </ul>
                </div>
                <div class="col-lg-7 col-md-7 col-sm-12">
                    <div class="input-group m-b-0">
                        <div class="col-12">
                            <div class="card">
                                <div class="body">
                                    <div class="row">
                                        
                                        <div class="col-6" style="text-align:left">

                                            <div id="exportGrids"></div>
                                        </div>
                                        <div class="col-6" style="text-align: right  ">

                                     
                                            @(Html.DevExtreme().TextBox()
                                                .Value(Model.CompName).ReadOnly(true)
                                                )

                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row clearfix">

            <div class="col-md-12 col-lg-12">
                <div class="card">
                    <div class="body">

                        <div class="row">
                             
                            <div class="col-md-12 col-lg-12">
                                <div class="row clearfix">
                                    <div class="col-md-6 col-lg-6">
                                        <div class="card">
                                            <div class="body">
                                                <div class="row">

                                                    @(Html.DevExtreme().PivotGrid<DashPivotView>()
                                                        .ID("pivotgrid")
                                                        .AllowSortingBySummary(true)
                                                        .AllowFiltering(true)
                                                        .ShowBorders(true).AllowExpandAll(true)
                                                        .ShowColumnGrandTotals(false).AllowExpandAll(true)
                                                        .ShowRowGrandTotals(false)
                                                        .ShowRowTotals(false) 
                                                        .OnCellPrepared("cell_prepared")
                                                        .ShowColumnTotals(false).FieldChooser(fc => fc
                                                        .AllowSearch(false)
                                                        .ApplyChangesMode(ApplyChangesMode.Instantly)).FieldPanel(e => e.ShowColumnFields(true).ShowDataFields(false).ShowFilterFields(false).ShowRowFields(false))

                                                        .DataSource(d => d
                                                        .Store(s => s.RemoteController().LoadUrl(Url.Page(null, "MarkupMarjinA")).LoadParams(new { compid = new JS("function() {return mcompid; }") }))
                                                        .Fields(fields =>
                                                        {
                                                            fields.AddFor(m => m.GroupName)
                                                            .Width(170)
                                                            .Area(PivotGridArea.Row).Expanded(true).Visible(false)
                                                            ; fields.AddFor(m => m.CounterZone)
                                                            .Width(120)
                                                            .Area(PivotGridArea.Row).SortBy(PivotGridSortBy.Value).Visible(false)
                                                            ;
                                                            fields.AddFor(m => m.AccountMainDescription)
                                                            .Width(170)
                                                            .Area(PivotGridArea.Row).SortBy(PivotGridSortBy.None).AllowFiltering(false)
                                                            ;
                                                      
                                                            fields.AddFor(m => m.Year).SortBy(PivotGridSortBy.Value).Area(PivotGridArea.Column).Expanded(true);
                                                            
                                                            fields.AddFor(m => m.Amount)
                                                            .SummaryType(SummaryType.Sum).Caption("Değer")
                                                            .DataType(PivotGridDataType.Number).Format(x => x.Precision(1)).Format(Format.FixedPoint)
                                                            .Area(PivotGridArea.Data);
                                                            fields.AddFor(m => m.Diff).SummaryType(SummaryType.Sum).Width(9)
                                                            .DataType(PivotGridDataType.Number).Format(x => x.Precision(1)).Caption("Oran(%)")
                                                            .Area(PivotGridArea.Data);
                                                        })
                                                        )
                                                        )



                                                </div>


                                            </div>
                                        </div>


                                    </div>
                                    <div class="col-md-6 col-lg-6">
                                        <div class="card">
                                            <div class="body">
                                                <div class="row">

                                                    @(Html.DevExtreme().PivotGrid<DashPivotView>()
                                                        .ID("pivotgrida")
                                                        .AllowSortingBySummary(true)
                                                        .AllowFiltering(true)
                                                        .ShowBorders(true).AllowExpandAll(true)
                                                        .ShowColumnGrandTotals(false).AllowExpandAll(true)
                                                        .ShowRowGrandTotals(false)
                                                        .ShowRowTotals(false) 
                                                        .OnCellPrepared("cell_prepared")
                                                        .ShowColumnTotals(false).FieldChooser(fc => fc
                                                        .AllowSearch(false)
                                                        .ApplyChangesMode(ApplyChangesMode.Instantly)).FieldPanel(e => e.ShowColumnFields(true).ShowDataFields(false).ShowFilterFields(false).ShowRowFields(false))

                                                        .DataSource(d => d
                                                        .Store(s => s.RemoteController().LoadUrl(Url.Page(null, "MarkupMarjinB")).LoadParams(new { compid = new JS("function() {return mcompid; }") }))
                                                        .Fields(fields =>
                                                        {
                                                            fields.AddFor(m => m.GroupName)
                                                            .Width(170).Visible(false)
                                                            .Area(PivotGridArea.Row).Expanded(true)
                                                            ; fields.AddFor(m => m.CounterZone)
                                                            .Width(120)
                                                            .Area(PivotGridArea.Row).SortBy(PivotGridSortBy.Value).Visible(false)
                                                            ;
                                                            fields.AddFor(m => m.AccountMainDescription)
                                                            .Width(170)
                                                            .Area(PivotGridArea.Row).SortBy(PivotGridSortBy.None).AllowFiltering(false)
                                                            ;

                                                            fields.AddFor(m => m.Year).SortBy(PivotGridSortBy.Value).Area(PivotGridArea.Column).Expanded(true);

                                                            fields.AddFor(m => m.Amount)
                                                            .SummaryType(SummaryType.Sum).Caption("Değer")
                                                            .DataType(PivotGridDataType.Number).Format(x => x.Precision(1)).Format(Format.FixedPoint)
                                                            .Area(PivotGridArea.Data);
                                                            fields.AddFor(m => m.Diff).SummaryType(SummaryType.Sum).Width(9)
                                                            .DataType(PivotGridDataType.Number).Format(x => x.Precision(1)).Caption("Oran(%)")
                                                            .Area(PivotGridArea.Data);
                                                        })
                                                        )
                                                        )



                                                </div>


                                            </div>
                                        </div>


                                    </div>
                                </div>



                            </div>

                        </div>



                    </div>

                </div>

            </div>




        </div>





    </div>
</section>


@section Scripts {
    <script src="/js/a_poly.js"></script>
    <script src="/js/a_excel.js"></script>
    <script src="/js/a_filesave.js"></script>

    <script>
        var formatCurrency = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 2 }).format;

        function cell_prepared(e) {
            var fieldData = e.value;
            var fieldHtml = "";

       
            // if (e.area == 'data' && e.cell.columnPath[0] == 2021 ) {
            //    // e.cellElement.css('background-color', 'red');
            // }
            // if (e.area == 'data' ) {
            //     console.log(e.cell.columnPath[0]);
            //     console.log(e.cell.columnType);
            //     console.log(e.cell.rowPath[0]);
            // }
            // if (fieldData && fieldData.Value) {
            //     if (fieldData.Diff) {
            //         options.cellElement.addClass((fieldData.Diff > 0) ? "inc" : "dec");
            //         fieldHtml += "<div class='current-value'>" +
            //             formatCurrency(fieldData.Value) +
            //             "</div> <div class='diff'>" +
            //             Math.abs(fieldData.Diff).toFixed(2) +
            //             "  </div>";
            //     } else {
            //         fieldHtml = fieldData.Value;
            //     }
            //     options.cellElement.html(fieldHtml);
            // }
        }
    
        function getfdate() {
            //
            var usrdate = sessionStorage.getItem("fnckpyear");
            if (usrdate == null || usrdate == undefined || usrdate == "null") {


                usrdate = "";
            }
            else {
                return usrdate;
            }

        };
        var myearcount = "@Model.YearCount";
        var iNum = (parseInt(myearcount)*3);
        var tchek = getfdate();

        $("#exportGrids").dxButton({
            text: "Excel Raporu",
            onClick: function () {
                var dataGrid1 = $("#pivotgrid").dxPivotGrid("instance");
                var dataGrid2 = $("#pivotgrida").dxPivotGrid("instance");
                var workbook = new ExcelJS.Workbook();
                var worksheet = workbook.addWorksheet('Main sheet');

                Object.assign(
                    worksheet.getRow(2).getCell(2),
                    { value: "AKTİFLER", font: { bold: true, size: 16, underline: 'double' } }
                );
                Object.assign(
                    worksheet.getRow(2).getCell(iNum),
                    { value: "PASİFLER", font: { bold: true, size: 16, underline: 'double' } }
                );

                DevExpress.excelExporter.exportPivotGrid({
                    worksheet,
                    component: dataGrid1,
                    topLeftCell: { row: 4, column: 2 }
                }).then(function (dataGridRange) {
                    return DevExpress.excelExporter.exportPivotGrid({
                        worksheet,
                        component: dataGrid2,
                        topLeftCell: { row: 4, column: (dataGridRange.to.column + 2) }
                    });
                }).then(function () {
                    workbook.xlsx.writeBuffer().then(function (buffer) {
                        saveAs(new Blob([buffer], { type: "application/octet-stream" }), "BalanceSheet.xlsx");
                    });
                });
            }
        });

        // function onExporting(e) {
        //     var workbook = new ExcelJS.Workbook();
        //     var worksheet = workbook.addWorksheet('balancesheet');

        //     DevExpress.excelExporter.exportPivotGrid({
        //         component: e.component,
        //         worksheet: worksheet
        //     }).then(function () {
        //         workbook.xlsx.writeBuffer().then(function (buffer) {
        //             saveAs(new Blob([buffer], { type: 'application/octet-stream' }), 'balancesheet.xlsx');
        //         });
        //     });
        //     e.cancel = true;
        // }
    </script>

    <script>
        var tyear = $("#txtmyear").val();
        var mcompid = $("#txtmcomp").val();
        function getLoadPanelInstance() {
            return $("#loadPanel").dxLoadPanel("instance");
        }
        function loadPanel_shown(e) {
            setTimeout(function () {
                e.component.hide();
            }, 7000);
        }
        var misready = "nok";
        var chkReadyState = setInterval(function () {
            if (document.readyState == "complete") {

                clearInterval(chkReadyState);

                misready = "ok";
            }
        }, 100);

        function getselectYearInstance() {
            return $("#slctYear").dxSelectBox("instance");
        }
       
        function selectYear_Initialized() {
            getselectYearInstance().option("value", parseInt($("#txtmyear").val()));
            var chkco = parseInt($("#txtyearcount").val());

            if (chkco < 2) {
                getselectYearInstance().option("readOnly", true);
            }


        }
 

        function selectYear_valueChanged(data) {
            var slctval = data.selectedItem.MYear;
            var turl = '/admin/dashbilanco?handler=GraphYear&nyear=' + slctval;

            if (misready == "ok" && tyear.toString() != slctval.toString()) {
                getLoadPanelInstance().show();
                $.ajax({
                    type: "GET",
                    url: turl,
                    contentType: "application/json; charset=utf-8",
                    dataType: "json",
                    success: function (response) {
                        if (response == "ok") {
                            location.reload();
                        }
                    },
                    failure: function (response) {
                        alert(response);
                    }
                });
            }

        }

   


        var dt1 = "105";
    </script>
} 