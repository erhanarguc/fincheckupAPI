@page

@using fincheckup.Models.Hvvn
@using fincheckup.Models.NKolay.UploadArea
@using fincheckup.Models.ViewM
@model IndexModel
@{
    ViewData["Title"] = "Giriş Ekranı";
    Layout = "~/Pages/Shared/_LayoutFirm.cshtml";
}

@using fincheckup.ENTITY
@functions {
    public class IndexModel : PageModel
    {
        public IEnumerable<YearResult> myearResult;
        public long UserID;
        public long CompID;
        public int CompCount;
        public int YearCount;
        public long curcomID;
        public string CompName;
        public int curcomCount;
        public DataViewerMain mrequestDataViewer;
        public HhvnUsers CurrentUser;
         
        
        public IEnumerable<Companies> mreqListCompany;
        

        public string currentcompname { get; set; }

        public void OnGet()
        {
            myearResult = YearResult.getValue();



            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            CurrentUser = HhvnUsers.GetRow_User(UserID);
            mreqListCompany = Companies.Getby_User(UserID);
            currentcompname = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault().CompanyName;
            CompName = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault().CompanyName;
            curcomID = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault().ID;
            curcomCount = mreqListCompany.Count();
            //currentUpload = UploadMain.Get_Data(DateTime.Now.Year, curcomID);

            CompID = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault().ID;
            YearCount = TBLXml.GetYearByComapnyID(CompID);
            CompCount = mreqListCompany.Count();

        }


        public JsonResult OnGetSalerMainChk(DataSourceLoadOptions options, int monthid)
        {
            mrequestDataViewer = new DataViewerMain();
            if (monthid < 1)
            {
                return new JsonResult(DataSourceLoader.Load(mrequestDataViewer.EntryData, options));
            }

            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            CurrentUser = HhvnUsers.GetRow_User(UserID);

            curcomID = Companies.Getby_User(UserID).Where(x => x.IsDefault == 1).FirstOrDefault().ID;

            mrequestDataViewer.SetDataViewer(MainDash.DataViewerMainSource(CurrentUser.SelectedYear, curcomID, monthid));

            return new JsonResult(DataSourceLoader.Load(mrequestDataViewer.EntryData, options));

        }
        public JsonResult OnGetSalerMainZeta(DataSourceLoadOptions options, int monthid)
        {
            mrequestDataViewer = new DataViewerMain();
            if (monthid < 1)
            {
                return new JsonResult(DataSourceLoader.Load(mrequestDataViewer.EntryData, options));
            }


            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            CurrentUser = HhvnUsers.GetRow_User(UserID);

            curcomID = Companies.Getby_User(UserID).Where(x => x.IsDefault == 1).FirstOrDefault().ID;


            mrequestDataViewer.SetDataViewer(MainDash.DataViewerMainSource(CurrentUser.SelectedYear, curcomID, monthid));

            return new JsonResult(DataSourceLoader.Load(mrequestDataViewer.EntryData, options));

        }
        public JsonResult OnGetSalerDate(DataSourceLoadOptions options)
        {
            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            CurrentUser = HhvnUsers.GetRow_User(UserID);

            curcomID = Companies.Getby_User(UserID).Where(x => x.IsDefault == 1).FirstOrDefault().ID;
            List<int> chkErrormonthID = MainDash.Get_DatabyError(CurrentUser.SelectedYear, curcomID).Where(x => x.TErrorRowCount == 0).Select(x => x.MainMonth).ToList();
            var currentUploadM = UploadMain.Get_Data(CurrentUser.SelectedYear, curcomID);

            currentUploadM.Where(x => chkErrormonthID.Contains(x.MainMonth)).Select(c => { c.ErrorCount = -1; return c; }).OrderBy(x => x.MainMonth).ToList();
            return new JsonResult(DataSourceLoader.Load(currentUploadM, options));

        }
        public JsonResult OnGetSalerYear(DataSourceLoadOptions options)
        {


            var YearSetm = YearResult.getValue().OrderByDescending(x => x.MYear);
            return new JsonResult(DataSourceLoader.Load(YearSetm, options));

        }
        public JsonResult OnGetSalerComp(DataSourceLoadOptions options)
        {

            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            mreqListCompany = Companies.Getby_User(UserID);
            return new JsonResult(DataSourceLoader.Load(mreqListCompany, options));

        }

       
        public JsonResult OnGetGraphYear(int nyear)
        {
            UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            HhvnUsers.SetYear(nyear, UserID);
            return new JsonResult("ok");
        }

    }
}
@section Styles {
    <script src="~/assets/progress.js"></script>

    <style>
        .orangecolor {
            background-color: #874651;
        }

        .greencolor {
            background-color: #004aad; 
        }

        /*   .dx-scrollview-content {
            position: absolute;
        }*/

        #grdContYearDefter .centerdiv {
            vertical-align: middle !important;
            font-weight: bold;
            font-size: 13px;
            color: whitesmoke;
        }

        #grdContYearDefter .dx-header-row .centerdiv {
            vertical-align: middle !important;
            font-weight: bold;
            font-size: 13px;
            color: black;
        }
    </style>
}
<input type="hidden" style="display:none" id="txtcompname" value="@Model.currentcompname" />
<input type="hidden" style="display:none" id="txtcurcomCount" value="@Model.curcomCount" />
<input type="hidden" style="display:none" id="txtcurcomide" value="@Model.curcomID" />
<section class="content home">
    <div class="container-fluid">
        <div class="block-header">
            <div class="row clearfix">
                <div class="col-lg-5 col-md-5 col-sm-12">
                    <h2>Dashboard</h2>
                    <ul class="breadcrumb padding-0">
                        <li class="breadcrumb-item"><a href=""><i class="zmdi zmdi-home"></i></a></li>
                        <li class="breadcrumb-item active">Dashboard</li>
                    </ul>
                </div>
                <div class="col-lg-7 col-md-7 col-sm-12">
                    <div class="input-group m-b-0">
                        <div class="col-12">
                            <div class="card">
                                <div class="body">
                                    <div class="row">
                                        <div class="col-6" style="text-align:left">
                                            @(Html.DevExtreme().SelectBox()
                    .DataSource(Model.myearResult).ID("slctYear").OnInitialized("selectYear_Initialized")
                    .DisplayExpr("MText").OnSelectionChanged("selectYear_valueChanged")
                    .ValueExpr("MYear").Width(105)
                )

                                        </div>
                                        <div class="col-6" style="text-align:right">

                             @(Html.DevExtreme().TextBox()
                                .Value(Model.CompName)  .ReadOnly(true)
                            )

                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row clearfix">
            <div class="col-lg-6 col-md-6">
                <div class="card">
                    <div class="body l-parpl text-center">

                        <h3 class="m-b-0 m-t-10 text-white"><span id="txterroredcombin">0</span></h3>
                        <span class="text-white">Uyarı Alan Satırlar(Tekil Kombine Edilmiş)</span>
                    </div>
                </div>
            </div>
            <div class="col-lg-6 col-md-6">
                <div class="card">
                    <div class="body l-seagreen text-center">

                        <h3 class="m-b-0 m-t-10 text-white"><span id="txtrowcount">0</span></h3>
                        <span class="text-white">Toplam Satır</span>
                    </div>
                </div>
            </div>

        </div>
        <input type="hidden" style="display:none" id="txtmyear" value="@Model.CurrentUser.SelectedYear" />
        <input type="hidden" style="display:none" id="txtmcomp" value="@Model.CompID" />
        <input type="hidden" style="display:none" id="txtyearcount" value="@Model.YearCount" />
        <input type="hidden" style="display:none" id="txtcompcount" value="@Model.CompCount" />
        <input type="hidden" style="display:none" value="0" id="txtisupdate" />
        <div class="row clearfix">
            <div class="col-lg-12 col-md-12">

                <script>
                    var tyear = $("#txtmyear").val();
                    var mcompid = $("#txtmcomp").val();
                    function onCellPrepared(e) {

                        if (e.columnIndex == 4 && e.rowType == "data") {
                            var chkk = e.data;

                            if (chkk.TxResult == "Bekleniyor" || parseInt(chkk.ErrorCount) < 0) {
                                
                                e.cellElement.find(".dx-link-editb").remove();
                                e.cellElement.find(".dxmain").remove();
                            }
 
                        }


                    }
                    var misready = "nok";
                    var chkReadyState = setInterval(function () {
                        if (document.readyState == "complete") {

                            clearInterval(chkReadyState);

                            misready = "ok";
                        }
                    }, 100);
                    function onRowPrepared(info) {
                        if (info.rowType != "header" && info.data.TxResult == "Bekleniyor") {

                            info.rowElement.addClass("orangecolor");
                        } else if (info.rowType != "header") {
                            info.rowElement.addClass("greencolor");
                        }
                    }
                    var dt1 = "0";
                    var compid = $("#txtmcomp").val();
                    var monthid = 0;
                    var year = $("#txtmyear").val();
                    function onItemClick(data) {

                        $("#mainyear").val(data.MainYear);
                        $("#mainmonth").val(data.MainMonth);

                        if (data.TxResult == "Bekleniyor") {
                            $("#txtisupdate").val("0");
                            $('#uploadInput').focus().trigger('click');

                        }
                        else {

                            Swal.fire({
                                title: 'Daha Önce Yüklenen E-Defteri değiştirmek istiyor musunuz?',
                                showDenyButton: true,
                                confirmButtonText: 'Evet',
                                denyButtonText: 'Hayır',
                            }).then((result) => {
                                /* Read more about isConfirmed, isDenied below */
                                if (result.isConfirmed) {
                                    $("#txtisupdate").val("1");
                                    $('#uploadInput').focus().trigger('click');
                                } else if (result.isDenied) {
                                    Swal.fire('Değiştirme İşlemi İptal Edildi', '', 'info')
                                }
                            })
                        }


                    }


                    function getselectYearInstance() {
                        return $("#slctYear").dxSelectBox("instance");
                    }
                    
                    function selectYear_Initialized() {
                        getselectYearInstance().option("value", parseInt($("#txtmyear").val()));
                        var chkco = parseInt($("#txtyearcount").val());

                        //if (chkco < 2) {
                        //    getselectYearInstance().option("readOnly", true);
                        //}


                    }

                   

                    function selectYear_valueChanged(data) {
                        var slctval = data.selectedItem.MYear;
                        var turl = '/admin/dashboard?handler=GraphYear&nyear=' + slctval;

                        if (misready == "ok" && tyear.toString() != slctval.toString()) {
                            getLoadPanelInstance().show();
                            $.ajax({
                                type: "GET",
                                url: turl,
                                contentType: "application/json; charset=utf-8",
                                dataType: "json",
                                success: function (response) {
                                    if (response == "ok") {
                                        location.reload();
                                    }
                                },
                                failure: function (response) {
                                    alert(response);
                                }
                            });
                        }

                    }

                   
                    //     function onItemClickPopupComp(data) {
                    //         var chkcommp = $("#txtcurcomCount").val()
                    //         if (chkcommp == 1 || chkcommp == "1") {
                    //             return;
                    //         }


                    //         var compnm = data.CompanyName;
                    //         var zcompid = data.ID;
                    //         var databtn = $("#buttonComp").dxButton("instance");
                    //         databtn.option("text", compnm.toString());
                    //         const popupz = $("#popupComp").dxPopup("instance");
                    //         popupz.hide();
                    //         $("#txtcurcomide").val(zcompid);

                    ///*         var slctval = data.selectedItem.ID;*/
                    //         console.log("selectCompany_valueChanged   =>" + zcompid);
                    //         var turl = '/admin/upload?handler=GraphComp&ncompid=' + zcompid;
                    //         if (compid.toString() != zcompid.toString()) {

                    //             $.ajax({
                    //                 type: "GET",
                    //                 url: turl,
                    //                 contentType: "application/json; charset=utf-8",
                    //                 dataType: "json",
                    //                 success: function (response) {
                    //                     compid = zcompid;
                    //                     let elementz = document.getElementById("grdContYearDefter");
                    //                     let instancez = DevExpress.ui.dxDataGrid.getInstance(elementz);
                    //                     instancez.getDataSource().reload();
                    //                 },
                    //                 failure: function (response) {
                    //                     alert(response);
                    //                 }
                    //             });
                    //         }


                    //         /*   $("#grdContYearDefter").dxDataGrid("instance").getDataSource().reload();*/

                    //     }

                    //function onItemClickPopup(data) {
                    //    var chkyear = data.MYear;
                    //    var databtn = $("#buttonYear").dxButton("instance");
                    //    databtn.option("text", chkyear.toString());
                    //    const popupz = $("#popup").dxPopup("instance");
                    //    popupz.hide();
                    //    year = chkyear;

                    //    /*   $("#grdContYearDefter").dxDataGrid("instance").getDataSource().reload();*/
                    //    let elementz = document.getElementById("grdContYearDefter");
                    //    let instancez = DevExpress.ui.dxDataGrid.getInstance(elementz);
                    //    instancez.getDataSource().reload();
                    //}
                    function funcUpload() {
                        $('#uploadInput').focus().trigger('click');
                        $("#griddiv").css("display", "none");
                    }


                    function funcClose() {
                        const popuprz = $("#popupgrid").dxPopup("instance");
                        popuprz.hide();
                    }
                    //function funcYear() {
                    //    const popupz = $("#popup").dxPopup("instance");
                    //    popupz.show();
                    //}
                    //function funcComp() {
                    //    const popupz = $("#popupComp").dxPopup("instance");
                    //    popupz.show();
                    //}
                    function readURL(input) {

                        var reader = new FileReader();
                        let elementz = document.getElementById("grdContYearDefter");
                        let instancez = DevExpress.ui.dxDataGrid.getInstance(elementz);
                        let element = document.getElementById("grdContainerDefter");
                        let instance = DevExpress.ui.dxDataGrid.getInstance(element);
                        compid = $("#txtcurcomide").val();




                        if (input.files && input.files[0]) {

                            var fdatam = new FormData();
                            var tttdate = $("#mainmonth").val() + "_" + $("#mainyear").val();
                            for (var i = 0; i < input.files.length; i++) {
                                fdatam.append('file', input.files[i]);
                            }

                            reader.onload = function (e) {

                                var imgur = e.target.result;
                            };
                            reader.readAsDataURL(input.files[0]);
                            fdatam.append('id', 0);
                            fdatam.append('Caption', tttdate);
                            fdatam.append('ide', $("#txtcurcomide").val());
                            fdatam.append('idexml', "0");
                            startit();

                            if ($("#txtisupdate").val() == "0" || $("#txtisupdate").val() == 0) {
                                $.ajax({
                                    url: "/JsonService/Main/moodUpload",
                                    method: "POST",
                                    statusCode: {
                                        500: function () {
                                            getLoadPanelInstance().hide();
                                            Swal.fire({
                                                title: 'Server Erişim Sorunuz. Lütfen Sayfayı Yenileyiniz...',
                                                confirmButtonText: 'Yenile'
                                            }).then((result) => {
                                                /* Read more about isConfirmed, isDenied below */
                                                if (result.isConfirmed) {

                                                    location.reload();
                                                }
                                            });
                                        },
                                        404: function () {
                                            alert("Script 404");
                                        }
                                    },
                                    contentType: false,
                                    data: fdatam,
                                    processData: false,
                                    cache: false,
                                    success: function (data) {
                                        if (data == "nok") {
                                            //swal.close();
                                            getLoadPanelInstance().hide();
                                            document.getElementById('uploadInput').value = null;
                                            Swal.fire(
                                                'Hata!',
                                                'E-defter Yüklenemedi Lütfen Seçili Tarih ile E-Defter tarihini / E-Defter Entegratör Tipini KOntrol Ediniz!',
                                                'error'
                                            )
                                        }
                                        else {
                                            var arr = data.split('_');
                                            dt1 = arr[0];

                                            $("#txtrowcount").text(arr[1]);
                                            $("#txterroredcombin").text(arr[2]);

                                            monthid = $("#mainmonth").val();


                                            instance.getDataSource().reload();
                                            instancez.getDataSource().reload();
                                            $("#griddiv").css("display", "block");

                                            //swal.close();
                                            getLoadPanelInstance().hide(); document.getElementById('uploadInput').value = null;
                                            Swal.fire(
                                                'Başarılı!',
                                                'E-defter Yüklendi!',
                                                'success'
                                            )
                                        }


                                    }
                                });
                            }
                            else {
                                $.ajax({
                                    url: "/JsonService/Main/moodUploadUpdate",
                                    method: "POST",
                                    statusCode: {
                                        500: function () {
                                            getLoadPanelInstance().hide();
                                            Swal.fire({
                                                title: 'Server Erişim Sorunuz. Lütfen Sayfayı Yenileyiniz...',
                                                confirmButtonText: 'Yenile'
                                            }).then((result) => {
                                                /* Read more about isConfirmed, isDenied below */
                                                if (result.isConfirmed) {

                                                    location.reload();
                                                }
                                            });
                                        },
                                        404: function () {
                                            alert("Script 404");
                                        }
                                    },
                                    contentType: false,
                                    data: fdatam,
                                    processData: false,
                                    cache: false,
                                    success: function (data) {
                                        if (data == "nok") {
                                            //swal.close();
                                            getLoadPanelInstance().hide(); document.getElementById('uploadInput').value = null;
                                            Swal.fire(
                                                'Hata!',
                                                'E-defter Yüklenemedi Lütfen Seçili Tarih ile E-Defter tarihini / E-Defter Entegratör Tipini KOntrol Ediniz!',
                                                'error'
                                            )
                                        }
                                        else {
                                            var arr = data.split('_');

                                            dt1 = arr[0];
                                            $("#txtrowcount").text(arr[1]);
                                            $("#txterroredcombin").text(arr[2]);
                                            monthid = $("#mainmonth").val();


                                            instance.getDataSource().reload();
                                            instancez.getDataSource().reload();
                                            $("#griddiv").css("display", "block");

                                            //swal.close();
                                            getLoadPanelInstance().hide(); document.getElementById('uploadInput').value = null;
                                            Swal.fire(
                                                'Başarılı!',
                                                'E-defter Yüklendi!',
                                                'success'
                                            )
                                        }


                                    }
                                });
                            }

                        }
                    }



                    function startit() {
                        getLoadPanelInstance().show();
                 

                    };
                    function functionsec(data) {

                        monthid = data;
                        $("#mainmonth").val(monthid);
                        const popuprz = $("#popupgrid").dxPopup("instance");
                        popuprz.show();
                        let elementz = document.getElementById("grdErroredDefter");
                        let instancez = DevExpress.ui.dxDataGrid.getInstance(elementz);
                        instancez.getDataSource().reload();
                    }
                    function functionupdate(data) {
                        startit();
                        monthid = data;
                        var fdatam = new FormData(); 
                        fdatam.append('companyid', $("#txtcurcomide").val());
                        fdatam.append('month', data);
                        fdatam.append('year', $("#mainyear").val());
                     
                        $.ajax({
                            url: "/JsonService/Main/moodUpdate",
                            method: "POST",
                            statusCode: {
                                500: function () {
                                    getLoadPanelInstance().hide();
                                    Swal.fire({
                                        title: 'Server Erişim Sorunuz. Lütfen Sayfayı Yenileyiniz...',
                                        confirmButtonText: 'Yenile'
                                    }).then((result) => {
                                        /* Read more about isConfirmed, isDenied below */
                                        if (result.isConfirmed) {

                                            location.reload();
                                        }
                                    });
                                },
                                404: function () {
                                    alert("Script 404");
                                }
                            },
                            contentType: false,
                            data: fdatam,
                            processData: false,
                            cache: false,
                            success: function (data) {
                                getLoadPanelInstance().hide();
                                if (data == "nok") {
                         
                                    Swal.fire(
                                        'Hata!',
                                        'Hata ile Karşılaşıldı!',
                                        'error'
                                    )
                                }
                                else {
                                    var arr = data.split('_');
                              
                                    dt1 = arr[0];
                                   
                                    Swal.fire(
                                        'Başarılı!',
                                        'Güncelleme Tamamlandı!',
                                        'success'
                                    )
                                }


                            }
                        });
                      
                      
                    }

                </script>






                <div class="card">
                    <div class="body">

                        <div class="row">

                            <div class="col-md-12 col-lg-7">
                                <div class="col-md-12 col-lg-12">
                                    <div class="card" id="griddiv" style="display:none">
                                        <div class="header">


                                        </div>
                                        <div class="body">
                                            @(Html.DevExtreme().DataGrid<DataViewer>().ID("grdContainerDefter").ColumnHidingEnabled(true).RemoteOperations(true)
    .Scrolling(s => s
        .Mode(GridScrollingMode.Virtual)
        .RowRenderingMode(GridRowRenderingMode.Virtual)
    )
    .Paging(p => p.PageSize(100)).AllowColumnResizing(true)
    .ColumnMinWidth(50)
    .ColumnAutoWidth(true)
    .HeaderFilter(f => f.Visible(false))
    .WordWrapEnabled(true).Export(e => e.Enabled(true)).OnExporting("exporting")
    .Height(410)
    .ShowBorders(true).DataSource(d => d.RemoteController().LoadUrl(Url.Page(null, "SalerMainChk")).LoadParams(new {monthid = new JS("function() {return monthid; }") })).Columns(columns =>
    {
        columns.AddFor(m => m.DocumentDate).Caption("DocumentDate").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.EnteredBy).Caption("EnteredBy").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.EntryNumber).Caption("EntryNumber").CssClass("centerdiv").Format(Format.FixedPoint);
        columns.AddFor(m => m.AccountMainID).Caption("AccountMainID").CssClass("centerdiv");
        columns.AddFor(m => m.AccountMainDescription).Caption("AccountMainDescription").Visible(false).Width(0);
        columns.AddFor(m => m.AccountSubID).Caption("AccountSubID").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.AccountSubDescription).Caption("AccountSubDescription").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DebitCreditCode).Caption("DebitCreditCode").CssClass("centerdiv");
        columns.AddFor(m => m.Amount).Caption("Amount").CssClass("centerdiv").Format(Format.Decimal);
        columns.AddFor(m => m.DetailComment).Caption("DetailComment").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DocumentTypeDescription).Caption("DocumentTypeDescription").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.PaymentMethod).Caption("PaymentMethod").CssClass("centerdiv").Visible(false); 
        columns.AddFor(m => m.ColorDesc).Caption("c").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.Description).Caption("Muhasebe Denetimi").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.ColorDescTax).Caption("v").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DescriptionTax).Caption("Vergi Denetimi").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.ColorDescInside).Caption("i").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DescriptionInside).Caption("İç Denetimi").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.GroupName)
           .GroupIndex(0).Caption("");

    }).Grouping(grouping => grouping.AutoExpandAll(true)).GroupPanel(groupPanel => groupPanel.Visible(false))
                        )




                                        </div>

                                    </div>
                                </div>

                            </div>
                            <div class="col-md-12 col-lg-5">
                                <div class="row clearfix">
                                    <div class="col-md-12 col-lg-12">


                                        <div class="card">
                                            <div class="body">
                                                <div class="row">
                                                    @(Html.DevExtreme().DataGrid<YearlyUploadResult>().ColumnHidingEnabled(true).ID("grdContYearDefter").OnCellPrepared("onCellPrepared").OnRowPrepared("onRowPrepared").ShowColumnLines(false).ShowBorders(false).ColumnAutoWidth(true)
    .Height(440) .Columns(columns =>
    {
    columns.AddFor(m => m.MainYear).Caption("Yıl").CssClass("centerdiv") ;
    columns.AddFor(m => m.MainMonth).Visible(false).SortOrder(SortOrder.Asc);
    columns.AddFor(m => m.DocumentMonthTr).Caption("Ay").CssClass("centerdiv") ;
    columns.AddFor(m => m.TxResult).Caption("Durum").CssClass("centerdiv") ;
 
            columns.Add().CssClass("").CellTemplate(@<text>
                                                    <div   onclick="functionupdate('[%= data.MainMonth %]')" class="dxmain dx-button dx-button-normal dx-button-mode-contained dx-widget dx-button-has-text" aria-label="Yükle" tabindex="0" role="button"><div class="dx-button-content"><i class="zmdi zmdi-refresh"></i></div></div>


                  </text>);
    columns.Add().CssClass("").CellTemplate(@<text>
                                                    <div   onclick="functionsec('[%= data.MainMonth %]')" class="dx-link-editb dx-button dx-button-normal dx-button-mode-contained dx-widget dx-button-has-text" aria-label="Yükle" tabindex="0" role="button"><div class="dx-button-content"><span class="dx-button-text">Uyarılar</span></div></div>


                  </text>);

        columns.AddFor(m => m.ErrorCount).Visible(false);

    }).DataSource(d => d.RemoteController().LoadUrl(Url.Page(null, "SalerDate"))
                        ))
                                                </div>


                                            </div>
                                        </div>
                                    </div>

                                </div>



                            </div>

                        </div>



                    </div>

                </div>
            </div>

        </div>



    </div>
</section>
@(Html.DevExtreme().LoadPanel()
        .ID("loadPanel")
        .ShadingColor("rgba(0,0,0,0.4)")
        .Position(p => p.Of("#employee"))
        .Visible(false)
        .ShowIndicator(true).Message("E-defter İşleniyor.. Lütfen Bekleyiniz...")
        .ShowPane(true).Height(300).Width(500)
        .Shading(true)
          .HideOnOutsideClick(false)
        .OnShown("loadPanel_shown")
    )
<input type="text" id="mainyear" name="mainyear" value="@Model.CurrentUser.SelectedYear" style="display:none" />
<input type="text" id="mainmonth" name="mainmonth" style="display:none" />
<input type="file" id="uploadInput" multiple name="myFile" onchange="readURL(this);" style="display:none" accept=".xml, .csv, .zip" />

@(Html.DevExtreme().Popup()
    .ID("popupgrid")
    .Width(1150)
    .Height(515)
    .Visible(false).ShowCloseButton(true)
    .ShowTitle(false)
    .HideOnOutsideClick(true)
    .ContentTemplate(new TemplateName("popupgridtemp"))
)


@using (Html.DevExtreme().NamedTemplate("popupgridtemp"))
{@(Html.DevExtreme().Button().Icon("close")
        .ID("buttdonYear")
      .Width(35).Height(35)
        .Type(ButtonType.Default).OnClick("funcClose")
    )

    @(Html.DevExtreme().DataGrid<DataViewer>().ID("grdErroredDefter").ColumnHidingEnabled(true).RemoteOperations(true)
    .Scrolling(s => s
        .Mode(GridScrollingMode.Virtual)
        .RowRenderingMode(GridRowRenderingMode.Virtual)
    )
    .AllowColumnResizing(true)
    .ColumnMinWidth(50)
    .ColumnAutoWidth(true)
    .HeaderFilter(f => f.Visible(false))
    .WordWrapEnabled(true).Export(e => e.Enabled(true)).OnExporting("exportingz")
    .Height(435)
    .ShowBorders(true).DataSource(d => d.RemoteController().LoadUrl(Url.Page(null, "SalerMainZeta")).LoadParams(new {monthid = new JS("function() {return monthid; }") })).Columns(columns =>
    {
        columns.AddFor(m => m.DocumentDate).Caption("DocumentDate").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.EnteredBy).Caption("EnteredBy").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.EntryNumber).Caption("EntryNumber").CssClass("centerdiv").Format(Format.FixedPoint);
        columns.AddFor(m => m.AccountMainID).Caption("AccountMainID").CssClass("centerdiv");
        columns.AddFor(m => m.AccountMainDescription).Caption("AccountMainDescription").Visible(false).Width(0);
        columns.AddFor(m => m.AccountSubID).Caption("AccountSubID").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.AccountSubDescription).Caption("AccountSubDescription").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DebitCreditCode).Caption("DebitCreditCode").CssClass("centerdiv");
        columns.AddFor(m => m.Amount).Caption("Amount").CssClass("centerdiv").Format(Format.Decimal);
        columns.AddFor(m => m.DetailComment).Caption("DetailComment").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DocumentTypeDescription).Caption("DocumentTypeDescription").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.PaymentMethod).Caption("PaymentMethod").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.ColorDesc).Caption("c").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.Description).Caption("Muhasebe Denetimi").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.ColorDescTax).Caption("v").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DescriptionTax).Caption("Vergi Denetimi").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.ColorDescInside).Caption("i").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.DescriptionInside).Caption("İç Denetimi").CssClass("centerdiv").Visible(false);
        columns.AddFor(m => m.GroupName)
           .GroupIndex(0).Caption("");

    }).Grouping(grouping => grouping.AutoExpandAll(true)).GroupPanel(groupPanel => groupPanel.Visible(false))
                        )

}

@section Scripts{
    <script src="~/js/polyfill.min.js"></script>
    <script src="~/js/exceljs.min.js"></script>
    <script src="~/js/FileSaver.min.js"></script>

    <script>
        function getLoadPanelInstance() {
            return $("#loadPanel").dxLoadPanel("instance");
        }
        function loadPanel_shown(e) {
            setTimeout(function () {
                e.component.hide();
            }, 610000);
        }
        var txcompname = $("txtcompname").val();

        function formatMessage(e) {

            return $("#txtcompname").val();
        }
        function exporting(e) {
            e.component.beginUpdate();
            e.component.columnOption('DocumentDate', 'visible', true);
            e.component.columnOption('DetailComment', 'visible', true);
            e.component.columnOption('EnteredBy', 'visible', true);
            e.component.columnOption('DocumentTypeDescription', 'visible', true);
            e.component.columnOption('PaymentMethod', 'visible', true);
            e.component.columnOption('AccountMainDescription', 'visible', true);
            e.component.columnOption('AccountSubDescription', 'visible', true);
            e.component.columnOption('AccountSubID', 'visible', true);
            e.component.columnOption('ColorDesc', 'visible', true);
            e.component.columnOption('Description', 'visible', true);
            e.component.columnOption('ColorDescTax', 'visible', true);
            e.component.columnOption('DescriptionTax', 'visible', true);
            e.component.columnOption('ColorDescInside', 'visible', true);
            e.component.columnOption('DescriptionInside', 'visible', true);
            var workbook = new ExcelJS.Workbook();
            var worksheet = workbook.addWorksheet('Uyarı Alan Kayıtlar');

            worksheet.columns = [
                { width: 1 }, { width: 25 }, { width: 25 }, { width: 15 }, { width: 15 }, { width: 35 }, { width: 25 }, { width: 35 }, { width: 15 }, { width: 15 }, { width: 35 }, { width: 15 }, { width: 15 }, { width: 5 }, { width: 55 }, { width: 5 }, { width: 155 }, { width: 5 }, { width: 155 }
            ];
            var chkktxt = "";
            DevExpress.excelExporter.exportDataGrid({
                component: e.component,
                worksheet: worksheet,
                keepColumnWidths: false,
                topLeftCell: { row: 2, column: 2 },
                customizeCell: function (options) {
                    var gridCell = options.gridCell;
                    var excelCell = options.excelCell;
                    var chknum = "";
                    if (gridCell.rowType === "data") {
                        if (gridCell.column.dataField === 'DocumentDate') {
                            chkktxt = $("#mainyear").val() + "_" + $("#mainmonth").val();
                        }


                        

                    }
                    if (gridCell.rowType === "group") {
                        excelCell.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: "BEDFE6" } };
                    }
                }
            }).then(function () {
                workbook.xlsx.writeBuffer().then(function (buffer) {
                    saveAs(new Blob([buffer], { type: "application/octet-stream" }), chkktxt + "_" + "kayitlar.xlsx");
                });
            });
            e.cancel = true;
        }

        function exportingz(e) {
            e.component.beginUpdate();
            e.component.columnOption('DocumentDate', 'visible', true);
            e.component.columnOption('DetailComment', 'visible', true);
            e.component.columnOption('EnteredBy', 'visible', true);
            e.component.columnOption('DocumentTypeDescription', 'visible', true);
            e.component.columnOption('PaymentMethod', 'visible', true);
            e.component.columnOption('AccountMainDescription', 'visible', true);
            e.component.columnOption('AccountSubDescription', 'visible', true);
            e.component.columnOption('AccountSubID', 'visible', true);
            e.component.columnOption('ColorDesc', 'visible', true);
            e.component.columnOption('Description', 'visible', true);
            e.component.columnOption('ColorDescTax', 'visible', true);
            e.component.columnOption('DescriptionTax', 'visible', true);
            e.component.columnOption('ColorDescInside', 'visible', true);
            e.component.columnOption('DescriptionInside', 'visible', true);
            var workbook = new ExcelJS.Workbook();
            var worksheet = workbook.addWorksheet('Uyarı Alan Kayıtlar');

            worksheet.columns = [
                { width: 1 }, { width: 25 }, { width: 25 }, { width: 15 }, { width: 15 }, { width: 35 }, { width: 25 }, { width: 35 }, { width: 15 }, { width: 15 }, { width: 35 }, { width: 15 }, { width: 15 }, { width: 5 }, { width: 55 }, { width: 5 }, { width: 155 }, { width: 5 }, { width:155 }
            ];
            var chkktxt = "";
            DevExpress.excelExporter.exportDataGrid({
                component: e.component,
                worksheet: worksheet,
                keepColumnWidths: false,
                topLeftCell: { row: 2, column: 2 },
                customizeCell: function (options) {
                    var gridCell = options.gridCell;
                    var excelCell = options.excelCell;
                    if (gridCell.rowType === "data") {
                        if (gridCell.column.dataField === 'DocumentDate') {
                            chkktxt = $("#mainyear").val() + "_" + $("#mainmonth").val();
                        }

 

                    }
                    if (gridCell.rowType === "group") {
                        excelCell.fill = { type: 'pattern', pattern: 'solid', fgColor: { argb: "BEDFE6" } };
                    }
                }
            }).then(function () {
                workbook.xlsx.writeBuffer().then(function (buffer) {
                    saveAs(new Blob([buffer], { type: "application/octet-stream" }), chkktxt + "_" + "kayitlar.xlsx");
                });
            });
            e.cancel = true;
        }
    </script>

}

